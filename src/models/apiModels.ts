export type Review = {
    restaurant: string;
    taste: number;
    texture: number;
    visualPresentation: number;
    reviewText: string;
}

export type Image = {
    img: string;
    title: string;
}